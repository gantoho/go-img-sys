version: '3.8'

services:
  image-sys:
    build:
      context: ..
      dockerfile: deployments/Dockerfile
    ports:
      - "3128:3128"
    volumes:
      - ./files:/app/files
      - ./logs:/app/logs
    environment:
      - GIN_MODE=release
    restart: unless-stopped
